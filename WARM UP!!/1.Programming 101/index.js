let  a = 20;
const b = "suman";

 a = 50
// let sum = a + b
let multiply = a * b ;
console.log(multiply);
console.log(b + " "+ a);

let firstName = "suman"
let secondName = "messi"

secondName = 9;

console.log(firstName + " " + secondName);

// boolean are true or false format
let boolean = [true, false]
console.log(boolean[0]);
console.log(boolean[1]);
 
// arrays consits of index and values
// in arrays we can mix and assign Strings, Arrays , Arrays in Arrays, Boolean , Objects in a single array
let arr = [1, 2, 3, 4, 5,[1, 2,3], 6,"suman", "messi" , true, false], 
// console.log(arr[5][1]);
 sum = arr[2]+arr[5][1];
console.log(sum);

// objects is consist of key value pairs
// and it is used with . assigning with the object
// in objects we can assign any key and with any values and pair them

let obj = {
    a : 'suman',
    b: 'messi',
    c : 'football',
    d : [1, 2, 3, 4, 5, 9],
    e: [1,2, 3,4 ,5, 6]
}
// where a: is a key and strings are the values assigned to the keys
// console.log(obj.a +" and " + obj.c + obj.d[5]); //concatenation

console.log(obj.d[1] + "8" + obj.c);










